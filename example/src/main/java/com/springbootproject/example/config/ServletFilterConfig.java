package com.springbootproject.example.config;



public class ServletFilterConfig {
	/*
	 * @SuppressWarnings({ "rawtypes", "unchecked" })
	 * 
	 * @Bean public FilterRegistrationBean siteMeshFilter() { FilterRegistrationBean
	 * filter = new FilterRegistrationBean<>(); filter.setFilter(new
	 * SitemeshFilter()); return filter; }
	 */
}
